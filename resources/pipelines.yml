resources:
  pipelines:
    ehr_writeback_pipeline:
      name: "ehr-writeback-pipeline"
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      libraries:
        - notebook:
            path: ../src/ehr_writeback/pipelines/writeback_pipeline.py
      configuration:
        ehr_writeback.catalog: ${var.catalog}
        ehr_writeback.schema: ${var.schema}
      continuous: false
      channel: PREVIEW
