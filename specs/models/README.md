# Specs Model Overview

This repository uses `specs/models/` for domain and operational concepts that drive implementation.

- `release/` contains CI and package-release concepts for publish automation
- `shared/` is reserved for reusable value objects if future capabilities need them

Operational capabilities may model workflows and states even when they do not expose an application API.
